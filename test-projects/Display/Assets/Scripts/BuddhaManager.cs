using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using MLAPI;

public class BuddhaManager : MonoBehaviour
{
    void Start()
    {
        // Spawn the host hand sphere
        //if (NetworkManager.Singleton.ConnectedClients.TryGetValue(NetworkManager.Singleton.LocalClientId, out var networkedClient))
        //{
        //    var player = networkedClient.PlayerObject.GetComponent<BuddhaPlayer>();
        //    if (player)
        //    {
        //        player.SpawnHostHandSphere();
        //    }
        //}
    }

    void Update()
    {
        
    }
}
